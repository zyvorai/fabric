# Published baselines

Drop harness JSON here after a recorded run. Filename convention:

```
YYYY-MM-DD-<host>-<fabric-version>.json
```

Do not commit `status: offline` placeholders as if they were measurements.
