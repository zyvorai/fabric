[daemon]
# Listen address for REST API
listen = "127.0.0.1:8080"
# CORS allowed origins (default: ["http://127.0.0.1:8080"])
# cors_origins = ["http://127.0.0.1:8080", "https://myhost.example.com"]

[storage]
# Path to store VM state
path = "/var/lib/vmspawnd"
# Path to store VM images
image_path = "/var/lib/vmspawnd/images"

[network]
# Default network bridge
bridge = "br0"

[auth]
# Enable JWT-based authentication (default: true)
enabled = true
# JWT signing secret: auto-generated and persisted to /var/lib/vmspawnd/.jwt_secret
# if not set here or via VMSPAWND_JWT_SECRET env var.
# jwt_secret = "..."
# SQLite user database path
# db_path = "/var/lib/vmspawnd/auth.db"
# Token lifetime in hours (default: 24)
# token_expiration_hours = 24
# Admin password: auto-generated and written to /var/lib/vmspawnd/.admin_password
# if not set here or via VMSPAWND_ADMIN_PASSWORD env var. Only used on first startup.
# default_admin_password = "..."
