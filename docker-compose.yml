services:
  vmspawnd:
    build: .
    container_name: vmspawnd
    ports:
      - "9080:8080"
    volumes:
      - vmspawnd-data:/var/lib/vmspawnd
    privileged: true
    restart: unless-stopped

volumes:
  vmspawnd-data:
