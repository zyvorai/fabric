apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "zyvor-fabricd-operator.fullname" . }}
  labels:
    {{- include "zyvor-fabricd-operator.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "zyvor-fabricd-operator.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "zyvor-fabricd-operator.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "zyvor-fabricd-operator.serviceAccountName" . }}
      containers:
      - name: operator
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        env:
        - name: ZYVOR_FABRICD_URL
          value: {{ .Values.zyvor-fabricd.url | quote }}
        {{- if .Values.zyvor-fabricd.tokenSecret }}
        - name: ZYVOR_FABRICD_TOKEN
          valueFrom:
            secretKeyRef:
              name: {{ .Values.zyvor-fabricd.tokenSecret | quote }}
              key: {{ .Values.zyvor-fabricd.tokenSecretKey | quote }}
        {{- end }}
        - name: NODE_NAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
        resources:
          {{- toYaml .Values.resources | nindent 10 }}
