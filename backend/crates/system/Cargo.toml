[package]
license = "Apache-2.0"
name = "zyvor-fabric-system"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror.workspace = true
tracing = "0.1"
zyvor-fabric-cgroup = { path = "../cgroup" }

[dev-dependencies]
