import type {ReactNode} from 'react';
import ComparePage from '@site/src/components/compare/ComparePage';

/** Homepage is the Fabric-first compare experience (matrix default: Fabric vs the field). */
export default function Home(): ReactNode {
  return <ComparePage />;
}
