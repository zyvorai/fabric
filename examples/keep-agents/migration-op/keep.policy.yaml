version: 1
default_egress: deny
allow:
  - host: "127.0.0.1"
    methods: [GET]
    action: fabric-read
    ask: never
  - host: "127.0.0.1"
    methods: [POST, PUT, DELETE]
    action: fabric-mutate
    ask: always
deny:
  - host: "*.onion"
taint:
  on_untrusted_page: block_egress_until_ask
