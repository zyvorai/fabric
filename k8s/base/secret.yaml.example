# Example only — prefer applying k8s/base/secret.yaml for lab defaults
# (admin / Admin@321). Override in production:
#
#   kubectl create secret generic zyvor-fabric-secrets \
#     --from-literal=admin-username=admin \
#     --from-literal=admin-password='YOUR_PASSWORD' \
#     --from-literal=jwt-secret="$(openssl rand -base64 32)" \
#     -n zyvor-fabric
apiVersion: v1
kind: Secret
metadata:
  name: zyvor-fabric-secrets
  namespace: zyvor-fabric
  labels:
    app: zyvor-fabric
type: Opaque
stringData:
  admin-username: "admin"
  admin-password: "Admin@321"
  jwt-secret: "change-me-to-a-long-random-string"
